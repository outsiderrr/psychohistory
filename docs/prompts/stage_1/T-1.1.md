# T-1.1 Project Scaffolding — L3 Starter Prompt

**Task ID**: T-1.1
**Stage**: 1 (L2-T1 Foundations)
**Type**: L3 leaf execution (formal ABC; first L3 to run governance pipeline)
**Branch**: `claude/T-1.1-scaffolding`
**Dependencies**: none
**Estimated effort**: 1–2 hours

This file is the **paste-ready L3 starter prompt**. The author starts a new Claude Code session in the project root, writes the literal first message:

> `请按 /docs/prompts/stage_1/T-1.1.md 的指示执行任务`

— and the L3 conversation reads this file and proceeds.

---

## Your role

You are the L3 executor for task T-1.1 Project Scaffolding of the Psychohistory V0.3 project. This is the first L3 task to run formal governance — it doubles as a dry-run for the entire ABC + PR + B-review + C-fix pipeline. Bias toward strict adherence to governance.

You operate under:
- Hierarchy: L1 master planning → L2-T1 stage planning → **you (L3 T-1.1)**
- ABC stage: **A only** for this conversation. B-stage runs in a separate Codex session; C-stage runs in a new Claude Code session pointed at this same worktree + branch.
- Out of scope for this conversation: B review, C fix, merge, downstream tasks.

## Required reading (in order)

Read every file in this list before writing any code or running any commands. Read fully — these files are short and important.

1. **`docs/L1-PLAN.md`** — L1 master plan. Especially §1 hierarchy, §2 DAG, §3 L3 slicing rules. Confirms that T1.1 has no upstream dependencies.
2. **`docs/STAGE_1_TASKS.md`** — Stage 1 task list. Find the T-1.1 entry; note the **acceptance criteria 1–8**, the **out-of-scope** list, and the **L2-视角 audit checklist** that will appear in B-stage `{{REVIEW_TARGET}}`.
3. **`docs/TDD-V0.32.md`** §2 (Tech Stack & Conventions). This is your spec for what the scaffolded project must look like — Solidity ^0.8.20 (toolchain 0.8.27), Foundry, OpenZeppelin v5 + OZ-Upgradeable v5, directory structure under `src/`. (V0.32 is the active TDD revision; V0.31 is archived but kept on disk for reference. V0.32 §2 adds an `Emergency pause` paragraph documenting `PausableUpgradeable` inheritance — this becomes load-bearing in T1.2 / T1.3 and downstream contracts, but does not affect the scaffolding step.)
4. **`docs/reviews/master_plan/2026-05-09_review_routine_governance.md`** §3.1 (A-stage protocol), §3.3 (C-stage rules — for context, not for action), §6 (file save paths), §7 (5 hard rules).
5. **`docs/DECISIONS.md`** ADR-0001 through ADR-0014. Brief read; these decisions do not directly constrain T1.1 scaffolding but establish the design context you'll implement toward in T1.2 / T1.3 and downstream contracts. ADR-0001~0007 ratified at L1.C / L2-T0; ADR-0008~0014 ratified at L1 CEO Review (2026-05-09), adding Brier alignment, int256 numerical, on-chain forecaster stats, per-bounty TVL cap, PausableUpgradeable inheritance, withdrawal time-lock, and the Qualified Proposition Standard.

## Your task

Bootstrap the Foundry project skeleton. **No protocol code** — only the development environment.

### Concretely

1. **Foundry initialization**
   - Run `forge init --no-commit --no-git` (the repo is already a git repo; we don't want forge to overwrite). Inspect what forge generated; remove the auto-generated `src/Counter.sol`, `test/Counter.t.sol`, `script/Counter.s.sol` files since we have our own structure planned.
   - If `forge init` won't run on a non-empty directory, manually create the structure.

2. **OpenZeppelin dependencies (git submodules, locked to commits)**
   - `forge install OpenZeppelin/openzeppelin-contracts@v5.x.x --no-commit` — pick the latest v5 release tag (check the OZ GitHub for the current); record the **commit hash** of the tag.
   - `forge install OpenZeppelin/openzeppelin-contracts-upgradeable@v5.x.x --no-commit` — same procedure.
   - `forge install foundry-rs/forge-std@<latest stable> --no-commit`.
   - Verify each submodule's `.gitmodules` entry resolves to a specific commit (NOT a floating branch).
   - In `.gitmodules`, the submodule's `branch` field should NOT be set, OR if set, the submodule HEAD must equal the lockfile-recorded commit. Use `git submodule status` to verify.

3. **Directory structure** — create as empty directories with `.gitkeep` files (since git doesn't track empty dirs):
   ```
   src/
     core/         # all proxy-upgradeable contracts (Treasury, BountyManager, ...)
     libraries/    # BrierMath, PsychohistoryTypes, Constants
     interfaces/   # IBountyManager, IPredictionEngine, ...
     mocks/        # MockUSDC, MockOracle, etc.
   test/           # forge test files (T2.1 onward)
   script/         # Deploy scripts (T6.1)
   ```

4. **`foundry.toml`** — author from scratch (or modify forge's default):
   - `[profile.default]`:
     - `solc_version = "0.8.27"`
     - `optimizer = true`
     - `optimizer_runs = 200`
     - `via_ir = false`
     - `src = "src"`, `out = "out"`, `libs = ["lib"]`, `test = "test"`, `script = "script"`
     - `cache_path = "cache"`
   - `[profile.default.fuzz]` — `runs = 256` for now (T2.1 will tune)
   - `[profile.default.invariant]` — `runs = 256`, `depth = 16` for now

5. **`remappings.txt`** — at project root:
   ```
   @openzeppelin/contracts/=lib/openzeppelin-contracts/contracts/
   @openzeppelin/contracts-upgradeable/=lib/openzeppelin-contracts-upgradeable/contracts/
   forge-std/=lib/forge-std/src/
   ```

6. **README.md** — one-line description is enough for now. Suggested:
   ```markdown
   # Psychohistory V0.3

   Decentralized prediction protocol with discrete + numerical events,
   Brier-scored top-50% cutoff settlement, three prize pools, and
   sponsor-funded analytics tier. See [docs/L1-PLAN.md](docs/L1-PLAN.md)
   and [docs/TDD-V0.32.md](docs/TDD-V0.32.md) for architecture.
   ```

7. **Verify** — `forge build` must succeed (no source files, but the toolchain must compile). `forge test` must succeed (no tests yet, but the harness must run).

8. **Commit** — single commit on `claude/T-1.1-scaffolding` branch. Suggested message:
   ```
   feat(T-1.1): Foundry scaffolding + OZ v5 + directory structure

   - forge init with solc 0.8.27, optimizer 200 runs
   - lib/ submodules: openzeppelin-contracts v5.x, openzeppelin-contracts-upgradeable v5.x, forge-std
   - src/{core,libraries,interfaces,mocks}, test/, script/ directories with .gitkeep
   - remappings.txt for OZ + forge-std
   - foundry.toml profiles (default, fuzz, invariant)
   - One-line README pointing to docs/L1-PLAN.md and docs/TDD-V0.32.md

   Refs: TDD V0.32 §2 (Tech Stack & Conventions); STAGE_1_TASKS.md T-1.1
   ```

9. **Push and open PR**:
   - `git push -u origin claude/T-1.1-scaffolding`
   - `gh pr create --base main --head claude/T-1.1-scaffolding --title "T-1.1 Project scaffolding" --body "<body below>"`
   - PR body template:
     ```markdown
     ## Task
     T-1.1 Project Scaffolding (Stage 1, L2-T1 Foundations)

     ## Summary
     Bootstrap the Foundry project skeleton: OpenZeppelin v5 + OZ-Upgradeable v5 + forge-std as locked submodules; src/test/script directory layout per TDD §2; foundry.toml profile with solc 0.8.27 + optimizer 200; remappings.txt; one-line README.

     No protocol code in this PR.

     ## Acceptance criteria
     - [x] forge init complete; auto-generated files removed
     - [x] OZ + OZ-Upgradeable + forge-std installed as locked submodules (commit hashes verified)
     - [x] Directory structure: src/{core,libraries,interfaces,mocks}, test/, script/ — populated with .gitkeep
     - [x] foundry.toml: solc 0.8.27, optimizer 200, sensible fuzz/invariant defaults
     - [x] remappings.txt: OZ + OZ-Upgradeable + forge-std
     - [x] .gitmodules committed; submodules locked to commits
     - [x] forge build succeeds
     - [x] forge test succeeds (zero tests but harness runs)

     ## Spec refs
     - TDD V0.32 §2 (Tech Stack & Conventions)
     - STAGE_1_TASKS.md → T-1.1
     - L1-PLAN.md §3.1 (T1.1 row of slicing table)

     ## Out of scope
     No contract source files; no test files (T2.1+); no deploy scripts (T6.1).
     ```

## Constraints

- **DO NOT** write any contract source code in `src/core/`, `src/libraries/`, or `src/interfaces/`. Those are subsequent tasks (T1.2, T1.3 for libraries+interfaces; later tasks for core).
- **DO NOT** commit `.claude/settings.local.json` (already in `.gitignore`).
- **DO NOT** modify any existing file under `docs/` — those are L1 / L2-T0 outputs and L1's source-of-truth. If you find a discrepancy that suggests a doc needs updating, flag it in the PR body and let L2 handle it; don't edit unilaterally.
- **DO NOT** open multiple PRs. Single PR for T-1.1.
- **DO NOT** merge the PR yourself. Merge happens after B + C + L2 acceptance per governance.
- **DO NOT** start B or C work in this conversation. A-stage only.

## When you're done

Final message to the author should report:

```
T-1.1 A stage complete.

Branch: claude/T-1.1-scaffolding
PR: <URL>
Final commit: <hash>
forge build: ✅
forge test: ✅
Submodules locked: <commit hashes>

Ready for B stage. Author next steps:
1. Cleanup any temp worktrees (`git worktree list` + remove)
2. Start fresh Codex session
3. Assemble B prompt from macOS Notes copy of REVIEW_PROMPT_CODE_GPT.md;
   substitute {{REVIEW_TARGET}} with the L2-视角 checklist from STAGE_1_TASKS.md
4. Codex completes review → pushes report to main
5. Author starts new Claude Code session for C stage on this same branch
```

## On failure

If something can't be done (e.g., a submodule install fails, forge version conflict, etc.):

1. Stop. Don't push partial work or open a half-done PR.
2. Report the specific failure to the author with full error message and what you've tried.
3. Wait for guidance. Don't assume a workaround.

If you discover a discrepancy in the spec (e.g., TDD §2 lists a constant that conflicts with another section), report it but proceed with the most defensible interpretation; flag the discrepancy in your final report so L2 can update the spec.
